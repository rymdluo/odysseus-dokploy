# MCP SDK (pulls starlette + sse-starlette for the SSE server); uvicorn runs it.
# Pinned to a major range for reproducible rebuilds (matches the ODYSSEUS_REF pin).
mcp>=1.2,<2
uvicorn[standard]>=0.27,<1
